// Placeholder so the wrapper target links a binary. The "Embed .NET MAUI app" build phase
// replaces it with the executable from the .NET MAUI build before Xcode signs the bundle.
int main(void) { return 0; }
